spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/points
    username: points
    password: points
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

pg:
  mock:
    fail-rate: ${PG_MOCK_FAIL_RATE:0.0}
    delay-ms: ${PG_MOCK_DELAY_MS:0}

sms:
  mock:
    fail-rate: ${SMS_MOCK_FAIL_RATE:0.0}
    delay-ms: ${SMS_MOCK_DELAY_MS:0}

email:
  mock:
    fail-rate: ${EMAIL_MOCK_FAIL_RATE:0.0}
    delay-ms: ${EMAIL_MOCK_DELAY_MS:0}

push:
  mock:
    fail-rate: ${PUSH_MOCK_FAIL_RATE:0.0}
    delay-ms: ${PUSH_MOCK_DELAY_MS:0}

kakao:
  mock:
    fail-rate: ${KAKAO_MOCK_FAIL_RATE:0.0}
    delay-ms: ${KAKAO_MOCK_DELAY_MS:0}

slack:
  mock:
    fail-rate: ${SLACK_MOCK_FAIL_RATE:0.0}
    delay-ms: ${SLACK_MOCK_DELAY_MS:0}

marketing-hub:
  mock:
    fail-rate: ${MARKETING_HUB_MOCK_FAIL_RATE:0.0}
    delay-ms: ${MARKETING_HUB_MOCK_DELAY_MS:0}

notification:
  channel:
    # 0 = 타임아웃 없음 (시나리오2-문제 상태), 양수 = ms 단위 타임아웃 (시나리오2-band-aid fix)
    timeout-ms: ${NOTIFICATION_CHANNEL_TIMEOUT_MS:0}
