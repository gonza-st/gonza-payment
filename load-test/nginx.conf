server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ =404;
    }

    # API 프록시 (리포트 페이지에서 잔액 조회용)
    location /api/ {
        proxy_pass http://api:8080/api/;
    }
}
